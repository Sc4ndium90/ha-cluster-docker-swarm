apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    # L'URL utilise le nom du service Docker pour communiquer en interne
    url: http://prometheus:9090
    isDefault: true
    editable: false
